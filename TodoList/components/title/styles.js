import { StyleSheet } from 'react-native';

import { blue, white } from '../../consts/colors.js';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: blue,
    marginTop: 20,
    padding: 5
  },
  text: {
    color: white,
    textAlign: 'center'
  }
})
